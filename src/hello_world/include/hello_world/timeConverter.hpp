#include <iostream>
#include <string>

using namespace std;

int dayOfTheWeek(int year, int month, int day);
double secondsDifference(int year0, int month0, int day0, int year1, int month1,
                         int day1);
int secondsDifferenceBetweenIEAndROS(int year, int month, int day);